while( cin >> i) /* ... */
// the condition is flase when cin.good() is false, i.e., one of cin.eof() cin.fail() cin.bad() is true;
// cin.eof() is true when end of file is read, cin.fail() is true when there is an issue with the device
// cin.fail() is true when for example incorrect datatype is read
