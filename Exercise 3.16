#include <iostream>
#include <vector>
using std::cout;
using std::endl;
using std::vector;
int main(){
    vector<int> ivec1;
    for(vector<int>::size_type i=0;i!=10;++i){
        ivec1.push_back(42);
    }
    vector<int> ivec2=ivec1;
    vector<int> ivec3(10,42);
    return 0;
}
/*
the third approach is the easiest!
*/
