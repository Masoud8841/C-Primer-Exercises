// array vs vector
// array performs faster but has no size function nor push_back (fixed size). 
// one can allocate more memory to have a long enough array but increases memory usage
// easier to debug codes with less error if vector is used
