#include <iostream>
int main(){
    int sum=0;
    for (int i=-100;i<=100;++i)
    sum+=i;
    std::cout << sum << std::endl;
    return 0;
}
// the code sums up all integeres between -100 and +100: the sum is zero.
