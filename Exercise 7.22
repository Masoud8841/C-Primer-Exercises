bool compare(matrix &, matrix &);
vector<int>::iterator change_val(int, vector<int>::iterator);
// based on the names, one can guess what function does and selects mmeaningful variable names in the prototypes
