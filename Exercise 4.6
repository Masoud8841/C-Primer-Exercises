#include <iostream>
using std::cin;
using std::cout;
using std::endl;

int main(){
    const size_t array_size=10;
    int ia[array_size];
    for(size_t i=1;i<=array_size;++i)
        ia[i]=i;
    return 0;
}
/*
gives runtime error. The program writes in ia[10] which is a memory segment outside of the array.
*/
