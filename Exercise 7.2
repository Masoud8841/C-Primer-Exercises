(a) int f(){
      string s;
      // ...
      return s;
    }
    // return type is string not int, one can change the return type of the function to string!
(b) f2 (int i){/* ... */} // no return type, add void for example
(c) int calc(int v1, int v1) /* ... */} // missing { and same same parameter names, use v2 and add {
(d) double square(double x) return x*x; // add curly braces around return x*x; expression.
